---
interface Props {
  label: string;
}
const { label } = Astro.props;
---
<div class="flex items-center gap-4 mb-3">
  <span class="font-mono text-[0.63rem] text-brand uppercase tracking-[0.3em]">{label}</span>
  <div class="w-12 h-px bg-brand opacity-40"></div>
</div>
